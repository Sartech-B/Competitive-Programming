#include <bits/stdc++.h>

using namespace std;

typedef struct node
{
	char name[60];
}dir;

int mx = 100000;

int main()
{
	freopen("in", "r", stdin);
	freopen("out", "w", stdout);
	int T;
	scanf("%d", &T);
	for(int tt=1; tt<=T; tt++)
	{
		printf("Case #%d: ");
		int n, m, graph[mx][mx];
		scanf("%d %d", &n, &m);
		
	}
}